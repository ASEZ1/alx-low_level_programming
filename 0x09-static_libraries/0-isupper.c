#include "main.h"

/**
 * _isupper - checks in  uppercase character
 * @c:  character to be checked
 * Return: 1 if c is uppercase, else 0 otherwise
 */
int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
